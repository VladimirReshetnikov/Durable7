[package]
name = "durable7-range-update"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Persistent measured sequences with logarithmic lazy range updates for Rust."

[lib]
name = "durable7_range_update"
path = "src/lib.rs"

[dependencies]
durable7-fingertree = { path = "../FingerTree" }
